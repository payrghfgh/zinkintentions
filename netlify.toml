[build]
  command = "sh build_keys.sh"
  publish = "."

[build.environment]
  # Default empty values so the build doesn't fail if variables aren't set yet
  FB_API_KEY = ""
  FB_AUTH_DOMAIN = ""
  FB_PROJECT_ID = ""
  FB_STORAGE_BUCKET = ""
  FB_MESSAGING_SENDER_ID = ""
  FB_APP_ID = ""
